version: 2

sources:
  - name: jaffle_shop
    database: raw
    config: 
      freshness:
        warn_after:
          count: 12
          period: hour
        error_after:
          count: 24
          period: hour
      loaded_at_field: _ETL_LOADED_AT
    tables:
      - name: customers
        config:
          freshness: null
      - name: orders